#include <bits/stdc++.h>
using namespace std;

// Adjacency matrix
// vector<vector<int> > adj_mat(vector<vector<int> > &mat, int n,int m){
//     return mat(n, vector<int>(m));
// }


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // Adjacency matrix
    // int n;
    // int m;
    // cin>>n>>m;
    // vector<vector<int> > adj_mat(n, vector<int>(m));

    // Ajacency list
    // int V;Ṣ
    // cin>>V;Ṣ
    // vector<int> adj_list[V];Ṣ
    // int u;Ṣ
    // int v;Ṣ
    // adj_list[u].push_back(v);Ṣ
    // adj_list[v].push_back(u);
    
    


    return 0;
}